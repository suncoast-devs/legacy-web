<div class="row">
  <% @organizations.each do |organization| %>

    <div class="organization col-md-12" style="background-color: <%= organization.color %>">
      <%= react_component('Organization', organization.attributes) %>
    </div>

  <% end %>
</div>
